<h1>All Authors</h1>

<p>We have <%= @authors.count %> authors.</p>

<ul>
  <% @authors.each do |author| %>
    <li>
      <%= "#{author.first_name} #{author.last_name}" %>
    </li>
  <% end %>
</ul>
